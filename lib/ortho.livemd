# Untitled notebook

## Section

```elixir
ExUnit.start(autorun: false)

defmodule PairTest do
  use ExUnit.Case, async: true

  test "it initializes" do
    o = Pair.new("a", "b")
    assert o.first == "a"
  end
end

ExUnit.run()
```
